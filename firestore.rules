rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORÄRE EMERGENCY RULES - ERLAUBT ALLES FÜR DEBUGGING
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}